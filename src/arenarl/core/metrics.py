"""
Metrics — Automatic data collection and export for training runs.

Features:
    - Auto-tracks episode rewards, lengths, and terminal states
    - summary(): Print human-readable training statistics
    - to_csv(path): Export raw data to CSV
    - to_json(path): Export raw data to JSON
    - plot(): Plot reward curve (requires matplotlib)
"""

# TODO: Implement Metrics class (Phase 3)
